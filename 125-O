/* the average time complexities of those operations on an AVL Tree (one answer per line): */

O(log(n))  /* Inserting the value n */
O(log(n))  /* Removing the node with the value n */
O(log(n))  /* Searching for a node in an AVL tree of size n */